package {{profilePackage}};

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;

import {{invokerPackage}}.{{classPrefix}}SettingActivity;
import org.deviceconnect.android.profile.SystemProfile;


class {{classPrefix}}SystemProfile: SystemProfile() {
    override fun getSettingPageActivity(request: Intent?, param: Bundle?): Class<out Activity?>? {
        // TODO 設定画面が不要な場合、null を返却してください.
        return {{classPrefix}}SettingActivity::class.java
    }
}
